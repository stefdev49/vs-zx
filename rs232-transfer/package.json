{
  "name": "rs232-transfer",
  "version": "1.0.0",
  "description": "RS232 transfer module for ZX Spectrum",
  "main": "out/index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "npx esbuild src/index.ts --bundle --platform=node --external:serialport --outfile=out/index.js --target=node18 --sourcemap",
    "test": "jest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "serialport": "^13.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.18.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
